import PartyPage from '@/app/landing/party/page';

export const metadata = {
  title: 'Party Booking — Little Wanderers',
  description: 'Birthday party holds and private events at Little Wanderers Play Studio & Cafe in West Hartford, CT.',
};

export default function PublicPartyPage() {
  return <PartyPage />;
}
