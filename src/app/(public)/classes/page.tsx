import ClassSchedulePage from '@/app/landing/classschedule/page';

export const metadata = {
  title: 'Classes — Little Wanderers',
  description: 'Kids classes and family programs at Little Wanderers Play Studio & Cafe in West Hartford, CT.',
};

export default function ClassesPage() {
  return <ClassSchedulePage />;
}
